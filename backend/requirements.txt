# Job Hunter backend. Deliberately small: everything else the product needs is
# in the standard library, so a first run does not depend on a large install.
fastapi==0.141.1
uvicorn==0.32.0
sqlalchemy==2.0.52
pydantic==2.13.5
httpx==0.27.2
python-multipart==0.0.19
